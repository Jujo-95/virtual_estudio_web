# Virtual Estudio Web (React + Vite)

Landing en React (single-page) para Virtual Estudio, con UI moderna (glass + gradientes) y copy basado en `RESUMEN_EJECUTIVO.md` y `runbook.md`.

## Desarrollo

- `npm install`
- `npm run dev`

## Build

- `npm run build`
- `npm run preview`
